# Aerin

Canonical agent instructions live in [AGENTS.md](AGENTS.md) — read it first (architecture, layering rules, Windows constraints, test/build commands).

The feature knowledge base is [docs/index.md](docs/index.md): one page per subsystem with mechanics, invariants, and source-file pointers. Read the relevant page before changing a subsystem, and update it in the same commit when behavior changes.
